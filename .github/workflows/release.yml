name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.22.0
          cache: npm
      - name: Install test runtime dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes ripgrep bubblewrap
      - run: npm ci --no-audit --no-fund
      - name: Validate release source
        run: |
          npm run check
          npx vitest run \
            test/providers-builtin.test.ts \
            test/provider-request-compatibility.test.ts \
            test/pi-backend.test.ts \
            test/m2-backend-contract.test.ts \
            test/ci-release.test.ts
      - name: Build release assets
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          test "$GITHUB_REF_NAME" = "v$VERSION"
          mkdir -p .artifacts
          npm pack --json --pack-destination .artifacts > .artifacts/pack.json
          node scripts/ci/verify-package.mjs .artifacts/pack.json
          cp ".artifacts/vspi-$VERSION.tgz" .artifacts/vspi-latest.tgz
          (cd .artifacts && sha256sum "vspi-$VERSION.tgz" vspi-latest.tgz > SHA256SUMS)
      - name: Install packaged CLI
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          PREFIX="$(mktemp -d)"
          npm install --global --prefix "$PREFIX" ".artifacts/vspi-$VERSION.tgz"
          test "$("$PREFIX/bin/vspi" --version)" = "$VERSION"
      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          gh release create "$GITHUB_REF_NAME" \
            ".artifacts/vspi-$VERSION.tgz" \
            .artifacts/vspi-latest.tgz \
            .artifacts/SHA256SUMS \
            --verify-tag \
            --title "VSPi $VERSION" \
            --notes-file "Docs/releases/v$VERSION.md"
